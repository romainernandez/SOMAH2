<PreferenceScreen
    xmlns:android="http://schemas.android.com/apk/res/android">

    <PreferenceCategory android:title="Language">
    <!-- NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it. -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:id="@+id/listPreference"
        android:defaultValue="0"
        android:entries="@array/pref_list_language"
        android:entryValues="@array/pref_example_list_language_values"
        android:key="languages_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:title="@string/pref_title_select_language" />
    </PreferenceCategory>
</PreferenceScreen>
